Connect: user "sysdba", server "localhost"
Execute: select *
from sel_client

Begin transaction: user "sysdba", server "localhost"
Execute: SELECT I.RDB$RELATION_NAME TABLE_NAME, I.RDB$INDEX_NAME INDEX_NAME, S.RDB$FIELD_NAME COLUMN_NAME, S.RDB$FIELD_POSITION COLUMN_POSITION, C.RDB$CONSTRAINT_NAME PKEY_NAME, I.RDB$UNIQUE_FLAG, C.RDB$CONSTRAINT_TYPE, I.RDB$INDEX_TYPE FROM RDB$INDICES I INNER JOIN RDB$INDEX_SEGMENTS S ON (I.RDB$INDEX_NAME = S.RDB$INDEX_NAME) LEFT JOIN RDB$RELATION_CONSTRAINTS C ON (I.RDB$INDEX_NAME = C.RDB$INDEX_NAME)  WHERE I.RDB$RELATION_NAME ='SEL_CLIENT' ORDER BY I.RDB$INDEX_NAME, S.RDB$FIELD_POSITION

Commit: user "sysdba", server "localhost"
